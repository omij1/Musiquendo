<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    app:cardElevation="@dimen/toolbar_elevation"
    app:cardBackgroundColor="@color/backgroundColor">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/playlist_item_image"
            style="@style/PlayListItemImage"/>

        <RelativeLayout
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="0.60">

            <TextView
                android:id="@+id/playlist_item_name"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                style="@style/PlayListItemName"/>

            <TextView
                android:id="@+id/playlist_item_creation"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_below="@id/playlist_item_name"
                style="@style/PlayListItemCreation"/>

        </RelativeLayout>

        <ImageButton
            android:id="@+id/playlist_download"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="0.10"
            android:src="@drawable/ic_file_download"
            style="@style/PlayListItemDownload"/>

    </LinearLayout>

</android.support.v7.widget.CardView>